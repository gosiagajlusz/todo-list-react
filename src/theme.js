import { ThemeProvider } from "styled-components";
import styled from "styled-components";

export const theme = {
  colors: {
    primaryColor: "#008080",
  },

  breakpoints: {
    mobile: 767,
  },
};
