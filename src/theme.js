import { ThemeProvider } from "styled-components";
import styled from "styled-components";

export const theme = {
  colors: {
    primaryColor: "#008080",
    black: "#000000",
  },

  breakpoints: {
    mobile: 767,
  },
};
